#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

echo "Running pre-push full validation (lint + type-check + unit tests)..."
npm run lint:check || exit 1
npm run type-check || exit 1
npm test || exit 1